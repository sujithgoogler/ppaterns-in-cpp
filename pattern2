#include <bits/stdc++.h>
using namespace std;
#define l "\n"
void pattern2(int n)
{
	for(int row=1;row<=n;row++)
	{
		for(int col=1;col<=n;col++)
		{
		   cout<<"*";
		}
		cout<<l;
	}
	
}
int main() {
	int n;
	cin>>n;
pattern2(n);

	return 0;
}
